"""The gate's tier 2 and tier 3: the checks that need a running game.

`bridge` is the connection, `scenarios` is what is asked of the game, and `tier2` is the runner that
starts a dedicated server, asks all of it, and shuts the server down again. See docs/plans/testing.md.
"""
