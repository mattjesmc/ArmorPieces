{
  "name": "armorpieces-blockbench-bridge",
  "version": "0.1.0",
  "description": "MCP proxy beside the mcp-toolkit shim: the nine Armor Pieces part tools (`kit`) or the eight skin tools (`kit_skin`), each with the plugin's check appended after every edit. Blockbench itself comes from the shim.",
  "type": "module",
  "private": true,
  "bin": {
    "armorpieces-bridge": "server.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0"
  }
}
